import Point from "./Point"

type Content = {
	//Данные модели (presentation)
	// position: Point,
	// layer: number,
	// type: number,
	position: Point,
	layer: number,
	type: number,
	uuid: string
}

export default Content